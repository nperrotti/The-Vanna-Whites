/* ===========================================
   Demo class for graphical user interface (GUI) basics using Swing.
   ===========================================*/

import javax.swing.*;

public class GUI extends JFrame {

    public GUI() {

        /*~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
          usage: Uncomment 1 line at a time, compile, run.
          Observe, make notes.
          Modify inputs, recompile, run.
          Observe, make notes.
          Q: What does each statement do? What do the inputs do?
          ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~*/

        //setTitle( "GUI Demo 01" );
        //setSize( 800, 200 );
        //setLocation( 100, 100 );
        //setDefaultCloseOperation( EXIT_ON_CLOSE );
    }


    public static void main( String[] args ) {

        GUI g = new GUI();
        //g.setVisible( true );
    }

}