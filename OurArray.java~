import java.util.ArrayList;
public class OurArray extends ArrayList {
    public String toString() {
	String retStr = "";
	for (int i = 0; i < this.size(); i++) {
	    retStr += this.get(i) + " ";
	}
    }
}